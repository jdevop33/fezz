name: Rollback Firebase Hosting
on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to rollback to (leave empty for previous version)'
        required: false
        default: ''

jobs:
  rollback:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Create Service Account File
        run: echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > ${{ github.workspace }}/service-account.json
        
      - name: Install Firebase CLI
        run: npm install -g firebase-tools
      
      - name: Rollback to Specific Version
        if: inputs.version != ''
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${{ github.workspace }}/service-account.json"
          firebase hosting:clone fezz-452821:${{ inputs.version }} fezz-452821:live
        
      - name: Rollback to Previous Version
        if: inputs.version == ''
        run: |
          export GOOGLE_APPLICATION_CREDENTIALS="${{ github.workspace }}/service-account.json"
          firebase hosting:clone fezz-452821:live^1 fezz-452821:live
          
      - name: Notify Success
        run: echo "Rollback completed successfully!"