specVersion: "v1beta"
serviceId: "fezz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fezz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
